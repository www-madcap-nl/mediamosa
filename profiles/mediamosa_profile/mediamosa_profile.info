; $Id:  $
name = "MediaMosa (Default installation)"
distribution_name = "MediaMosa"
description = "<p>
      MediaMosa is a full featured, webservice oriented media management
      and distribution platform. MediaMosa is implemented as a modular
      extension to the open source Drupal system. You'll find the MediaMosa
      specific modules in sites/all/modules, the remainder of the tree is
      the standard Drupal core.
    </p>
    <p>
      Please visit <a href=\"http://www.mediamosa.org\">http://www.mediamosa.org</a> for more information, announcements,
      documentation, community forums, and new releases.
    </p>
    <p>
    References:
    <ul>
      <li><a href=\"http://www.mediamosa.org\">http://www.mediamosa.org</a></li>
      <li><a href=\"http://www.drupal.org\">http://www.drupal.org</a></li>
    </ul>
  </p>
  <p>
    Use the database \"mediamosa\" example below to create your database before proceeding.
   </p>
    <code>
        # You have to create a user with a database:<br />
        CREATE USER mediamosa@'%' IDENTIFIED BY 'yourpasswd';<br />
        CREATE USER mediamosa@'localhost' IDENTIFIED BY 'yourpasswd';<br />
        GRANT USAGE ON * . * TO 'mediamosa'@'%' IDENTIFIED BY 'yourpasswd' WITH MAX_QUERIES_PER_HOUR 0 MAX_CONNECTIONS_PER_HOUR 0 MAX_UPDATES_PER_HOUR 0 MAX_USER_CONNECTIONS 0;<br />
        <br />
        CREATE DATABASE mediamosa DEFAULT CHARSET=utf8;<br />
        <br />
        GRANT ALL ON `mediamosa`.* TO 'mediamosa'@'%';<br />
    </code>
    <p>
        You may vary the mediamosa database prefix and the mediamosa user name.<br />
        You also have to give write access to the apache user to your MediaMosa (SAN/NAS) mount points, e.g. /srv/mediamosa.<br />
        <br />
        If you want to migrate your old 1.7 Mediamosa database to the new 2.x version, you have to create a new database user, which has enough rights to read your old databases.
    </p>"
version = 2.0
core = 7.x
dependencies[] = block
dependencies[] = comment
dependencies[] = color
dependencies[] = help
dependencies[] = menu
dependencies[] = dblog
dependencies[] = toolbar
dependencies[] = simpletest


; Using _profile extension to prevent name collisions with core module.
files[] = mediamosa_profile.profile
