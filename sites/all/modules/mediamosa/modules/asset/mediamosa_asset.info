; $Id $

name = "Asset"
description = "The asset module contains all submodules for asset like medium, metadata, access layer etc."
package = "MediaMosa 2 - Modules"
version = "2.0.0"
core = 7.x

; Dependencies (module names)
dependencies[] = mediamosa

; Used files.
files[] = mediamosa_asset.module
files[] = mediamosa_asset.rest.inc

; Media.
files[] = medium/mediamosa_asset_medium.rest.inc

; Medium Metadata.
files[] = medium/metadata/mediamosa_asset_medium_metadata.rest.inc

; Medium Play Proxy.
files[] = medium/play_proxy/mediamosa_asset_medium_play_proxy.rest.inc

; Metadata.
files[] = metadata/mediamosa_asset_metadata.rest.inc

; Metadata Property.
files[] = metadata/property/mediamosa_asset_metadata_property.rest.inc

; Metadata Property Group.
files[] = metadata/property/group/mediamosa_asset_metadata_property_group.rest.inc
