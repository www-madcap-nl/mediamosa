; $Id $

name = "Asset"
description = "The asset module contains all submodules for asset like mediafile, metadata, access layer etc."
package = "MediaMosa 2 - Modules"
version = "2.0.0"
core = 7.x

; Dependencies (module names)
dependencies[] = mediamosa

; Used files.
files[] = mediamosa_asset.module
files[] = mediamosa_asset.rest.inc

; Collection <-> Asset link table.
files[] = collection/mediamosa_asset_collection.rest.inc

; Media.
files[] = mediafile/mediamosa_asset_mediafile.rest.inc

; Mediafile Metadata.
files[] = mediafile/metadata/mediamosa_asset_mediafile_metadata.rest.inc

; Mediafile Play Proxy.
files[] = mediafile/play_proxy/mediamosa_asset_mediafile_play_proxy.rest.inc

; Mediafile Still.
files[] = mediafile/still/mediamosa_asset_mediafile_still.rest.inc

; Mediafile Upload.
files[] = mediafile/upload/mediamosa_asset_mediafile_upload.rest.inc

; Metadata.
files[] = metadata/mediamosa_asset_metadata.rest.inc

; Metadata Property.
files[] = metadata/property/mediamosa_asset_metadata_property.rest.inc

; Metadata Property Group.
files[] = metadata/property/group/mediamosa_asset_metadata_property_group.rest.inc

; Supplement.
files[] = supplement/mediamosa_asset_supplement.rest.inc
